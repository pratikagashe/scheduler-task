export enum FILE_SIZE_LIMIT {
    // All sizes are int bytes
    SIZE_300_KB = 300000,
    SIZE_1_MB = 1e6,
    SIZE_2_MB = 2e6,
}

export enum IMAGE_FILE_TYPE {
    PROFILE_IMAGE = 'profileImage',
}
